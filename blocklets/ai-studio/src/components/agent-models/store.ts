// import { getSupportedImagesModels, getSupportedModels } from '@blocklet/ai-runtime/common';
// import { ImageModelInfo, TextModelInfo } from '@blocklet/ai-runtime/types';
// import { create } from 'zustand';

// // !!!!!!!!!!!!!!!!!! project-specific models !!!!!!!!!!!!!!!!!!
// interface State {
//   activeAdapterId: string | null;
//   supportedImagesModels: ImageModelInfo[];
//   supportedTextModels: TextModelInfo[];
// }

// // export const recentModelsSelector = (state: State) => state.recentModels;

// export const useModelsStore = create<State>((set) => ({
//   activeAdapterId: null,
//   supportedImagesModels: [],
//   supportedTextModels: [],
// }));

// getSupportedImagesModels().then((models) => {
//   useModelsStore.setState({ supportedImagesModels: models });
// });

// getSupportedModels().then((models) => {
//   useModelsStore.setState({ supportedTextModels: models });
// });

export const x = 1;
